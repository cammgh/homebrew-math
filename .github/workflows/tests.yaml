name: Dynamic Tap Validation

on:
  push:
    branches:
      - main
      - maxima
      - acl2
      - fricas
      - axiom
      - hol88
  pull_request:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            arch: arm64
          - os: macos-15
            arch: x86_64

    runs-on: ${{ matrix.os }}
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      HOMEBREW_EVAL_ALL: "1"

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Homebrew Framework
        uses: Homebrew/actions/setup-homebrew@main

      # --- LAYER 1: MAIN BRANCH LOGIC (COMPILE & EXPORT GCL) ---
      - name: Build and Store Core GCL
        if: github.ref_name == 'main'
        run: |
          # 1. Dynamically extract the tap name registered by setup-homebrew
          TAP_NAME=$(brew tap | grep -v 'homebrew/' | head -n 1)
          echo "Detected Tap Name: $TAP_NAME"

          brew audit --formula "$TAP_NAME/gcl" --online
          brew install --formula --build-from-source "$TAP_NAME/gcl"
          brew test "$TAP_NAME/gcl"

          # Pack the compiled binary into a portable local Homebrew tarball archive (.bottle)
          brew bottle --root-url=https://localhost "$TAP_NAME/gcl"

      - name: Upload GCL Binary Package
        if: github.ref_name == 'main'
        uses: actions/upload-artifact@v4
        with:
          name: gcl-binary-${{ matrix.os }}-${{ matrix.arch }}
          path: gcl-*.tar.gz

      # --- LAYER 2: DERIVATIVE BRANCHES LOGIC (DOWNLOAD GCL & COMPILE DERIVATIVES) ---
      - name: Download Precompiled GCL from Main Branch
        if: github.ref_name != 'main'
        uses: dawidd6/action-download-artifact@v9
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          workflow: tests.yaml
          name: gcl-binary-${{ matrix.os }}-${{ matrix.arch }}
          workflow_conclusion: success
          path: ./
        continue-on-error: true # Falls back to source if no artifact exists yet

      - name: Install Local GCL Binary
        if: github.ref_name != 'main'
        run: |
          # 1. Dynamically extract the tap name registered by setup-homebrew
          TAP_NAME=$(brew tap | grep -v 'homebrew/' | head -n 1)
          echo "Detected Tap Name: $TAP_NAME"

          # If the download step caught the artifact, install instantly without a 40-minute compilation penalty
          if [ -f gcl-*.tar.gz ]; then
            brew install ./gcl-*.tar.gz
          else
            echo "No artifact found! Falling back to source compilation..."
            brew install --formula --build-from-source "$TAP_NAME/gc;"
          fi

      - name: Build Maxima Stack
        if: github.ref_name == 'maxima'
        run: |
          # 1. Dynamically extract the tap name registered by setup-homebrew
          TAP_NAME=$(brew tap | grep -v 'homebrew/' | head -n 1)
          echo "Detected Tap Name: $TAP_NAME"

          brew audit --formula "$TAP_NAME/maxima-gcl" --online
          brew install --formula --build-from-source "$TAP_NAME/maxima-gcl"
          brew test "$TAP_NAME/maxima-gcl"

      - name: Build ACL2 Stack
        if: github.ref_name == 'acl2'
        run: |
          # 1. Dynamically extract the tap name registered by setup-homebrew
          TAP_NAME=$(brew tap | grep -v 'homebrew/' | head -n 1)
          echo "Detected Tap Name: $TAP_NAME"

          brew audit --formula "$TAP_NAME/acl2-gcl" --online
          brew install --formula --build-from-source "$TAP_NAME/acl2-gcl"
          brew test "$TAP_NAME/acl2-gcl"

      - name: Build FriCAS Stack
        if: github.ref_name == 'fricas'
        run: |
          # 1. Dynamically extract the tap name registered by setup-homebrew
          TAP_NAME=$(brew tap | grep -v 'homebrew/' | head -n 1)
          echo "Detected Tap Name: $TAP_NAME"

          brew audit --formula "$TAP_NAME/fricas-gcl" --online
          brew install --formula --build-from-source "$TAP_NAME/fricas-gcl"
          brew test "$TAP_NAME/fricas-gcl"

      - name: Build HOL88 Stack
        if: github.ref_name == 'hol88'
        run: |
          # 1. Dynamically extract the tap name registered by setup-homebrew
          TAP_NAME=$(brew tap | grep -v 'homebrew/' | head -n 1)
          echo "Detected Tap Name: $TAP_NAME"

          brew audit --formula "$TAP_NAME/hol88-gcl" --online
          brew install --formula --build-from-source "$TAP_NAME/hol88-gcl"
          brew test "$TAP_NAME/hol88-gcl"

      - name: Build AXIOM Stack
        if: github.ref_name == 'axiom'
        run: |
          # 1. Dynamically extract the tap name registered by setup-homebrew
          TAP_NAME=$(brew tap | grep -v 'homebrew/' | head -n 1)
          echo "Detected Tap Name: $TAP_NAME"

          brew audit --formula "$TAP_NAME/axiom-gcl" --online
          brew install --formula --build-from-source "$TAP_NAME/axiom-gcl"
          brew test "$TAP_NAME/axiom-gcl"

      # --- DEB_BUG TRACE ARCHIVE ---
      - name: Open Debug SSH Session on Failure
        if: failure()
        uses: mxschmitt/action-tmate@v3
